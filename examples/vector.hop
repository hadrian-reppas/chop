import struct std::vector::{Vector}
import std::vector::{new_vector free_vector push pop}

fn sort_vector *Vector[float] {
    ~ // TODO
} 

fn main {
    0 cast_to[*float] new_vector
    for 0 to 20 {
        ~
        @ random push
    }
    @ sort_vector
    for 0 to 20 {
        ~ @ pop putln
    }
    free_vector
}
