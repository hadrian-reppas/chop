import int_helpers
import float_helpers::{print_twice}
import struct pair::{Pair}
import pair::{Pair .fst .snd}

fn print_twice bool {
    . putln putln
}

fn main {
    (double(one) + square(five_halves)) putln
    (one + 2*five_halves) putln
    "Hello, World!" putlns
    1 2 Pair .snd putln
}

fn one -> int {
    1
}

fn five_halves -> float {
    2.5
}

fn double int -> int {
    2 *
}

fn square float -> float {
    . *
}

fn add int int -> int {
    +
}
