global MALLOC: fn(int -> *byte)
global CRAZY: fn(fn(int) -> fn(byte))

// import std::vector
// import struct std::hash_map::{HashMap}

fn f[T] *fn(T) -> fn(T) {
    @ @
    ~ ~
    read
}

fn main {
    // vector::new[*byte] call
    // abort[int int int bool] assert ~ ~ ~
    0 cast_to[*fn(int)] f
    ~
}
